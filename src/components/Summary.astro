---
// Summary component for highlighting key takeaways
---

<div class="summary">
  <slot />
</div>

<style>
  .summary {
    margin: 2rem 0;
    padding: 1rem;
    background: var(--bg-card);
    border: 1px solid var(--border-secondary);
    border-radius: 8px;
    border-left: 4px solid var(--accent-primary);
  }

  .summary :global(p) {
    font-size: clamp(1.1rem, calc(1.1rem + 0.5vw), 1.3rem);
    line-height: clamp(1.6rem, calc(1.6rem + 0.5vw), 1.9rem);
    margin-bottom: 1rem;
    color: var(--text-tertiary);
  }

  .summary :global(p:last-child) {
    margin-bottom: 0;
  }

  @media (max-width: 768px) {
    .summary :global(p) {
      font-size: 1rem;
      line-height: 1.5rem;
    }
  }
</style>
