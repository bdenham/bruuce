---
// Vocabulary component for defining terms and concepts
---

<div class="vocabulary-container">
  <slot />
</div>

<style>
  .vocabulary-container {
    overflow-wrap: break-word;
    background: var(--bg-card);
    border: 1px solid var(--border-secondary);
    border-left: 4px solid var(--accent-primary);
    border-radius: 8px;
    padding: 1rem;
    margin: 1.5rem 0;
  }

  .vocabulary-container :global(h3) {
    color: var(--text-primary);
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0 0 0.5rem 0;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .vocabulary-container :global(h3::before) {
    content: '📚';
    font-size: 1rem;
  }

  .vocabulary-container :global(p) {
    margin: 0 0 1rem 0;
    color: var(--text-tertiary);
    line-height: 1.6;
  }

  .vocabulary-container :global(p:last-child) {
    margin-bottom: 0;
  }
</style>
